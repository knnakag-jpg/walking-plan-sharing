# このリポジトリについて

`index.html` は歩行会コース案（85コース）の単一HTML統合資料。第14章に、各コースのルート線を
実座標で描くインタラクティブマップを内蔵している。`tools/` に、ルート座標を検証・生成するNode製ツール一式がある。

## 地図・座標・旅程を触る前に必ず読むこと

**[Handover.md](Handover.md)** — 2026-08-29〜30に発生した「地図のルート線が建物・海を突っ切る直線になる」
「旅程記載距離と実測距離が最大4.5倍ズレる」事故の根本原因6件と、再発防止の具体的な手順・チェックリストをまとめてある。
新しいコースの追加、既存コースの座標修正、旅程文の編集のいずれも、着手前にこのファイルを読むこと。

## 特に致命的な禁止事項（Handover.mdの要約）

1. **ルーティングに `router.project-osrm.org/route/v1/foot/` を使わない。** 公開デモは車プロファイルへ
   無言フォールバックする（実測確認済み）。必ず Valhalla歩行者プロファイル（`valhalla1.openstreetmap.de/route`,
   `costing:"pedestrian"`）を使い、失敗時のみ BRouter hiking-beta にフォールバックすること。
2. **「ルーティング結果が旅程と乖離するときは直線を表示する」設計を作らない。** ズレを検出したら
   経路データを直すか、何も描画しない（バッジ表示のみ）のどちらか。直線フォールバックは絶対に禁止。
3. **曖昧な指示だけで座標を機械的にオフセットしない。** 「〇〇で延伸」等の文言だけを根拠に
   `直前の点+0.005/+0.002` のような座標をでっち上げない。実在する座標が分からなければ、その区間は作らない。
4. **座標を追加・変更したら、旅程本文（経路ノード文・旅程表）とカードの距離ラベルも同じコースについて
   必ず同時に見直す。** 座標だけ直して文章を放置すると「地図は合っているのに説明が嘘」という新種の不整合になる。
5. **新しいコースのカードを書いたら、その場で `var SERIES` 配列にも追加する。** カードとSERIESは
   `node tools/build-routes.mjs --verify` 相当のチェックで双方向の件数一致を確認できる。

## 提出前チェック

```bash
node tools/build-routes.mjs --verify
```
exit code が **0** であること（route-report.md にHARD gate違反が1件でもあれば exit 1）。
新しいコースを追加した場合の具体的な手順は [Handover.md](Handover.md) §4.2 を参照。
