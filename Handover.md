# Handover — 地図オーバーレイの再発防止ノウハウ

対象読者：このリポジトリ（`index.html`＝歩行会コース案の統合資料）に**新しいコースを追加・修正するすべてのAI／人間**。
Claude・DeepSeek・MuseSpark等、ツールを問わず作業前に一度目を通してください。

---

## 1. 何が起きていたか（インシデント概要）

2026-08-29以前、`index.html` の「統合インタラクティブマップ」で、コースのルート線が**建物・川・海を横切る直線**になっており、
人間が実際に歩けない経路が表示されていた。また、旅程表に書かれた「歩行：約X.Xkm」という距離と、
地図上の経路が実際に歩くと何kmになるかが、コースによっては **0.17倍〜4.5倍** も乖離していた。

厄介だったのは、**見た目には「地図に何か線が引かれている」ので一見動いているように見えた**こと。
Leafletの地図タイルは正常に表示され、マーカーも打たれ、線も引かれる。問題は「その線が意味のある経路か」という
一段階深いところにあり、目視だけでは（ズームして線を建物にめり込ませて確認しない限り）気づきにくい。

この事故は**単一の原因ではなく、独立した5〜6系統の不具合が重なって**発生した。1つ直しても他が残っていれば
同じ症状が再発する。以下、実際に検出した原因を「症状」「検出方法」「対処」の順で並べる。

---

## 2. 根本原因カタログ

### 原因1：ルーティングAPIが「徒歩」ではなく実質「車」だった

`router.project-osrm.org/route/v1/foot/`（OSRM公開デモの徒歩プロファイル）を使っていたが、
**このデモサーバは徒歩プロファイルを提供しておらず、`/foot/`が車プロファイルへ無言でフォールバックしていた。**

**検出方法**（実際に実行したコマンド）：
```bash
curl -s "https://router.project-osrm.org/route/v1/foot/139.7565,35.6890;139.7601,35.6860?overview=false"
curl -s "https://router.project-osrm.org/route/v1/driving/139.7565,35.6890;139.7601,35.6860?overview=false"
# → 2つのレスポンスが distance・weight_name まで完全に一致（weight_name:"routability" = 車プロファイルの印）
```
車道基準でルーティングされるため、園路・河川敷テラス・歩道橋・地下街・歩行者専用橋（例：多摩川スカイブリッジ歩行者デッキ）が
一切使われず、車道への大回りや「ルーティング結果が旅程と大きくズレる」状態を招いた。

**対処**：`https://valhalla1.openstreetmap.de/route`（`costing:"pedestrian"`）を第一候補、
失敗時のみ `https://brouter.de/brouter`（`profile=hiking-beta`）にフォールバック。
どちらもFOSSGISが公開しているCORS対応の無料デモで、`file://` から直接叩ける（動作確認済み）。

**⚠️ 二度と `router.project-osrm.org/route/v1/foot/` を使わないこと。** 名前に "foot" とあっても信用しない。

---

### 原因2：描画側が「乖離が大きいほど直線を表示する」設計になっていた

旧 `drawRoute()` / `drawRouteSolo()` にはこういうロジックがあった：

```js
var suspicious = (ratio > 1.7 && routedDist>2000) || (over > 2500 && ratio>1.35);
if (suspicious) {
  // ルーティング結果を捨てて、直線 fallback をそのまま残す
  return;
}
```

原因1でルーティング結果が実態と乖離するため `ratio` が膨らみ、**乖離が大きいコースほど確実に直線表示に
フォールバックする**という最悪の設計になっていた。「正しく検出したのに、検出結果を握りつぶして一番不正確な表示
（直線）を選ぶ」というのが直接の視覚バグの原因。

**対処**：直線フォールバックを完全に廃止した。今の実装（`drawEmbeddedRoute()`, index.htmlの`<script>`内）は、
**事前計算済みの実ルート（`ROUTE_GEOM`）が無ければ何も描かない**（「経路データ未生成」バッジを出すのみ）。
ルーティング結果に納得できないときの正しい振る舞いは「表示しない」であって「もっと不正確な線を出す」ではない。

**⚠️ 「うまくルーティングできなかったら直線で代替する」という設計は今後も絶対に作らないこと。**
ズレを検出する仕組み自体は良いが、検出した後にすることは「経路データを直す」か「表示しない」のどちらかのみ。

---

### 原因3：座標そのものが捏造・不正だった

**(a) 機械的なダミー延伸点**：81コース中43コースで、経路配列 `r` の最後に
「直前の点に一律 `+0.005緯度 / +0.002経度` を足しただけ」の、地図上に実在しない点が付いていた。
これは旅程文中の「平坦周回で延伸（V6・+0.8km）」という**曖昧な指示文をコードに落とし込む際、
実在の道を調べずに座標を機械的にオフセットしただけ**で済ませてしまったもの。

検出：
```js
function stripFakeTail(pts) {
  const a = pts[pts.length-2], b = pts[pts.length-1];
  const dLat = b[0]-a[0], dLng = b[1]-a[1];
  return Math.abs(dLat-0.005)<0.0006 && Math.abs(dLng-0.002)<0.0006; // trueなら捏造点
}
```

**(b) 水上・到達不能座標**：No.23（葛西臨海公園）・No.46（夢の島〜葛西）は座標が東京湾上にあり、
Valhallaに投げると `error_code:499 "GetTags: offset exceeds size of text list"` で失敗、BRouterは40km超の
迂回を返した＝ルーティングエンジンが「この座標には到達できない」と教えてくれていたのに、当時は無視されていた。

**(c) 数km単位の座標飛び**：例えばNo.36は「丸子橋」の次の点が実際には約1km離れた場所を指しており、
かつ順序が逆走していたため、地図上でジグザグになっていた。

**対処**：Nominatim（`https://nominatim.openstreetmap.org/search` / `/reverse`）で座標を検証してから使う。
検証手順は本ドキュメント末尾の「新しいコースを追加する手順」を参照。

**⚠️ 「〜方面へ延伸」「周回で+0.8km」のような曖昧な文言だけでコードを書き進めないこと。**
実在の座標が分からないなら、分かるところまでしか座標配列を作らない。曖昧な指示を機械的なオフセットで
埋めるくらいなら、その区間は未実装のままにする方が100倍マシ。

---

### 原因4：旅程の記載距離と地図の実測距離が検証されていなかった

カードに書かれた「歩行：約6.9km」等の数字と、実際にルーティングエンジンで測った距離を
**誰も一度も突き合わせていなかった**。結果、No.13（記載6.9km→実測1.14km、0.17倍）、
No.9（記載3.0km→実測13.5km、4.5倍）のような極端な乖離が長期間放置されていた。

**対処**：`tools/build-routes.mjs` が全コースについて実測距離と記載距離の比を自動計算し、
±7%（ハードゲートは±10%）を外れたら `FAIL` として `route-report.md` に出力する。
「記載距離が正、経路をそこまで実在の道で延伸／短縮する」という方針で、実際に**Valhallaの実ルート結果のみを使って**
（捏造座標なしで）距離を合わせた。詳細は次章のツール説明を参照。

---

### 原因5：カードは存在するのに地図データ（SERIES配列）に登録されていなかった

No.56〜59はカード本文・第13章のノード一覧・14-1番号対照表まで用意されていたのに、
JavaScriptの `var SERIES = [...]` 配列に一切追加されておらず、**地図上には最初から存在しなかった**。
「全59コース」という見出しの数字と実際に選択できるコース数が食い違っていても、誰も気づかなかった。

**対処**：`h3[id^="course-"]` の件数と `SERIES` 内の全コース件数が**双方向で一致する**ことを機械的に確認する
（後述のチェックリスト参照）。新しいコースのカードを書いたら、その場で必ず `SERIES` にも追記する。

---

### 原因6（2026-08-30に追加検出）：座標を直しても、旅程の「文章」側が古いままだった

原因3(a)を直した際、座標配列 `r` からは捏造延伸点を削除・実在座標での延伸に置き換えたが、
**カードの文章（経路ノード：… → 平坦周回で延伸（V6・+0.8km） → ゴール）は直さなかった**ため、
座標は直っても文章だけが「実際には存在しない延伸」を説明したまま残ってしまった
（54コースの経路ノード文、28コースの旅程表の行、計58コースで発生）。
さらに調査したところ、S1〜S8の「制作主専用修行案」では同種の文言が別表記（「平坦周回で延伸（最大7km）」）で
8コースに残っており、検索パターンを一つ決め打ちしただけでは見逃す実例だった。

さらに、距離を記載値に合わせる過程で**ルート上の経由点を間引いた（トリムした）コース**があり、
その際に旅程本文が言及している実在の立ち寄りスポット（例：No.5の「大國魂神社 参道・けやき並木」）が
座標配列からは消えているのに、本文にはまだ載ったままというケースも見つかった
（全件は洗い出せていない。詳細は§5「既知の未完了事項」）。

**教訓**：**「座標（地図）」「文章（旅程・経路ノード・旅程表）」「距離ラベル」は独立して劣化しうる3つのデータであり、
1つを直したら残り2つも必ず同じコースについて確認すること。** 座標だけ直して文章を放置すると、
今回のような「地図は合っているのに説明文が嘘をついている」という新しい種類の不整合を生む。

---

## 3. 禁止パターン集（実例つき）

新しいコースを書く・既存コースを直すときに、絶対にやってはいけないことの一覧。

| 禁止事項 | 悪い例（実際に検出したもの） | なぜダメか |
|---|---|---|
| OSRM公開デモの `/foot/` を使う | `router.project-osrm.org/route/v1/foot/...` | 車プロファイルへ無言フォールバックする |
| 乖離が大きいときに直線へフォールバックする描画ロジック | `if(suspicious){ /* 直線を残す */ return; }` | 検出したズレを握りつぶして最悪の表示を選ぶことになる |
| 曖昧な延伸指示を機械的座標オフセットで実装する | 直前の点に一律 `+0.005,+0.002` を加算 | 実在しない地点を指す。距離の帳尻合わせにしかならない |
| 座標を検証せずに使う | 東京湾上の座標（No.23旧版） | ルーティングが破綻する／そもそも人間が立てない場所 |
| 記載距離と実測距離を突き合わせない | 記載3.0km・実測13.5km（No.9旧版） | 旅程と地図が別のコースを指しているのと同じ |
| カード追加時にSERIES配列を更新し忘れる | No.56〜59（カードはあるがSERIES未登録） | 地図上にコースが一切現れない |
| 座標だけ直して経路ノード文・旅程表を放置する | 「平坦周回で延伸（V6・+0.8km）」の文言だけ残存（54件） | 座標は正しいのに説明文が嘘になる |
| 座標小数点3桁以下で妥協する | `[35.702, 139.540]`（約110m以上の誤差） | 隣のブロック・別の道を指してしまう精度 |

---

## 4. `tools/` ディレクトリの使い方

このリポジトリには、上記の不具合を機械的に検出・修正するための Node スクリプト一式がある
（外部依存なし、`node` があれば動く。全てVSCode上のBashから `node tools/xxx.mjs` で実行）。

### 4.1 現在も使う「ワークフロー本体」

| ファイル | 役割 |
|---|---|
| `extract-series.mjs` | `index.html` の `var SERIES = [...]` を読み取り `tools/waypoints-raw.json` に書き出す（＝現状の座標データを取り出す） |
| `extract-targets.mjs` | 各コースカードの「歩行：約X.Xkm」等を読み取り `tools/targets.json` に書き出す |
| `lib-polyline.mjs` | Valhalla方式のエンコード済みポリライン（precision 6）のエンコード／デコード共通処理 |
| `geocode.mjs` | Nominatim検索のワンショットCLI：`node tools/geocode.mjs "地名1" "地名2" ...` |
| `build-routes.mjs` | **中核**。`waypoints-raw.json` の各コースをValhalla（失敗時BRouter）で実ルーティングし、ゲート判定して `route-geom.json`（地図に埋め込む実データ）と `route-report.md`（合否レポート）を書き出す |
| `apply-to-html.mjs` | `waypoints-raw.json`（座標）と `route-geom.json`（ルート形状）を `index.html` の `SERIES` / `ROUTE_GEOM` ブロックへ書き戻す |

### 4.2 使い方（新しいコースを1本追加する場合）

```bash
# 1. index.htmlのカード本文を書く（h3 id="course-NN"、kvに「歩行：約X.Xkm」、経路ノード文）
#    このとき var SERIES 配列にも仮の座標でコースを追加する（後で上書きされる）

# 2. 現状を取り出す
node tools/extract-series.mjs      # → tools/waypoints-raw.json
node tools/extract-targets.mjs     # → tools/targets.json

# 3. tools/waypoints-raw.json を直接編集し、新コースの実座標を並べる
#    座標はNominatimで検証すること：
node tools/geocode.mjs "〇〇駅" "△△公園" "××橋"
#    出てきたlat/lonをそのまま r:[[lat,lng],...] に使う（丸めない・推測しない）

# 4. ルーティング＋検証＋埋め込みデータ生成（全コース分、1コースあたり約1.1秒）
node tools/build-routes.mjs
#    → tools/route-geom.json, tools/route-report.md が更新される
#    route-report.md で新コースの行が「OK」になっているか必ず確認する
#    「FAIL」なら課題（HARD列）を読んで waypoints-raw.json を直し、3〜4を繰り返す

# 5. index.html へ書き戻す
node tools/apply-to-html.mjs

# 6. 最終確認（exit 0が出るまでは未完成）
node tools/build-routes.mjs --verify
```

### 4.3 `--verify` の読み方

`route-report.md` の「課題（HARD）」列が空でその行が `OK` なら合格。「参考（soft）」列は目視用の参考情報
（座標精度がやや粗い、非隣接点の近接など）で、ビルド自体は失敗させない。**exit code**：
`node tools/build-routes.mjs --verify` が **0** で終わることが提出前の最低条件（1つでもHARD gate違反があると exit 1）。

ハードゲート（これを満たさないと `FAIL`）：
- 実測距離が記載距離（`歩行：約X.Xkm`）の **±10%以内**（推奨は±7%以内。7〜10%は soft warning）
- ルーティング自体が成功する（Valhalla/BRouterどちらも失敗＝座標が破綻している）
- 距離ラベルが読み取れる（カードに `歩行：約X.Xkm` 等が存在する）

### 4.4 過去の一括修正で使った“考古学的”スクリプト（参考用・再利用は非推奨）

`fix-distances.mjs` / `patch-courses.mjs` / `tune-remaining.mjs` / `fix-extension-text.mjs` /
`reverse-geocode-additions.mjs` は、2026-08-29〜30の一括修正時に**当時発見した特定コースの不具合専用**に
書いたワンオフスクリプト（特定のコース番号がハードコードされている）。動作原理（Valhallaで実測→過不足を
実在候補点で延伸／間引く→再検証、というアルゴリズム）は今後も参考になるが、**そのまま新しいコースに
使い回さないこと**。新規追加は必ず4.2の手順（手動で座標を用意→`build-routes.mjs`で検証）で行う。
`waypoints-original.json`・`diff-report.json`・`reverse-geocode-results.json`・`extension-text-plan.json` も
同じく当時の作業ログ（旧座標との差分の記録）であり、以後のワークフローでは参照不要。

---

## 5. 既知の未完了事項（今回のセッションで手が回らなかったこと）

- **旅程表の区間ごとの距離セル**（`<td class="c">0.6km</td>` のような各レグの距離）は、コース総距離を
  記載値に合わせる過程では更新したが、**レグ単位の内訳までは再計算していないコースがある**。
- 原因6で触れた「トリムで座標配列から消えた実在スポットが、旅程本文にはまだ載っている」ケースは、
  精査したのは経路ノード文の「延伸」フレーズ周辺のみで、**旅程表本体・見どころ欄まで全数チェックはしていない**。
  疑わしい候補：No.5（大國魂神社 参道・けやき並木が座標上は消えている）、No.6（所沢航空記念公園 東口／
  所沢航空発祥記念館）、No.59（柴崎町交差点・大学通り・殿ヶ谷戸庭園のうち一部）。他にもある可能性がある。
- 上記2点はいずれも「地図の線が建物や海を突っ切る」という今回の主症状ではなく、**旅程の説明文の精度**の問題。
  地図としては正しく実在の道を歩くようになっている。

新しいコースを追加するついでにこれらへ気づいたら、都度直してこのセクションから消してよい。

---

## 6. 参考

- 今回の修正の実測レポート：`tools/route-report.md`（全85コースの実測km・目標km・合否）
- 修正前バックアップ：`index.html.bak_v6_pre_map_fix`
- 履歴タブに保管されている改訂プロンプト2本（「4人歩く会用」「個人修行用」）は、本ドキュメントの内容を
  踏まえて2026-08-29にV2改定済み。プロンプトを他のAIツールに渡すときは、その本文と合わせて本ファイルの
  URL／パスも伝えること。
